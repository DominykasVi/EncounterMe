<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:map="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"
             xmlns:local="clr-namespace:MapApp;assembly=MapApp"
             mc:Ignorable="d"
             NavigationPage.HasNavigationBar="false"
             x:Class="MapApp.MainPage">

    <Grid>
        <!-- TODO Fix design, add absolute layout-->

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
 
 
        
        <local:CustomMap x:Name="MyMap" Grid.Row="0"
                   MapType="Street" />
        <!--<map:Map x:Name="MyMap" MapType="Street" HasZoomEnabled="True" Grid.Row="0">
            <map:Map.ItemTemplate>
                <DataTemplate>
                    <map:Pin Position="{Binding Position}"
                             Address="{Binding Address}"
                             Label="{Binding PlaceName }" />
                </DataTemplate>
            </map:Map.ItemTemplate>
        </map:Map>-->

        <Button Text="Search for location"
                VerticalOptions="End"
                x:Name="SearchButton"
                HorizontalOptions="Center"
                BackgroundColor="#6cd4ff"
                TextColor="#FFEEDD"
                BorderRadius="20"
                BorderWidth="2"
                WidthRequest="220"
                Clicked="ShowDashboard"
                Margin="10, 10, 10, 10"/>
        
        <Rectangle 
               x:Name="MainDashboard"
               Fill="#6cd4ff" 
               WidthRequest="300"
               HeightRequest="170"
               HorizontalOptions="Center"
               VerticalOptions="End"
               IsVisible="false" 
               Grid.Row="0"/>
        <!--
        <Label x:Name="SliderValue" TextColor="DarkOrange" VerticalOptions="End" HorizontalOptions="Center" Margin="0, 0, 0, 90"/>
        <Slider x:Name="RadiusSlider"
                Minimum="0"
                Maximum="1000"
                WidthRequest="300"
                BackgroundColor="DarkOrange"
                VerticalOptions="End"
                ValueChanged="SliderValueChanged"
                Grid.Row="0"
                Margin="75, 0, 75, 70"/>
        <Button Text="Search"
                Clicked="SearchForPlace"
                VerticalOptions="End"
                HorizontalOptions="Start"
                Margin="75, 0, 0, 23"
                 />
        <Button Text="ShowAll"
                Clicked="ShowAll"
                VerticalOptions="End"
                HorizontalOptions="End"
                Margin="0, 0, 75, 23"
                 />-->

        <Image x:Name="hamster"
				Source="hamster.png"
                  WidthRequest="10"
				 Margin="5, 5, 320, 740">
                <Image.GestureRecognizers>
                    <TapGestureRecognizer
                    Tapped="NavigateButton_OnClicked"
                    NumberOfTapsRequired="1" />
                </Image.GestureRecognizers>
            </Image>




        <!--<Grid x:Name="FilterPanel"
              Margin="75, 470, 75, 85"
              RowSpacing="0"
              ColumnSpacing="0">

        </Grid>-->
    </Grid>
</ContentPage>
